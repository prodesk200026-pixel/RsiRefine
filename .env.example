DHAN_CLIENT_ID=
DHAN_PIN=
DHAN_TOTP_SECRET=
# Recommended if you already have a valid Dhan access token: use this instead of TOTP.
# Dhan access tokens are time-limited, so replace it when it expires.
DHAN_ACCESS_TOKEN=
DEFAULT_INDEX=NIFTY
PORT=10000
